version: "3.8"

services:
  auth:
    build:
      context: ./auth
      dockerfile: Dockerfile
    networks:
      - app-network
    depends_on:
      - mongo-auth
    env_file:
      - .env
    environment:
      - MONGO_HOST=mongo-auth
      - MONGO_PORT=27017
      - MONGO_DATABASE=${AUTH_DB}

  mongo-auth:
    image: mongo:latest
    networks:
      - app-network
    volumes:
      - mongo-auth-data:/data/db

  models:
    build:
      context: ./models
      dockerfile: Dockerfile
    networks:
      - app-network
    depends_on:
      - mongo-models
    env_file:
      - .env
    environment:
      - MONGO_HOST=mongo-models
      - MONGO_PORT=27017
      - MONGO_DATABASE=${models_DB}

  mongo-models:
    image: mongo:latest
    networks:
      - app-network
    volumes:
      - mongo-models-data:/data/db

  storage:
    build:
      context: ./storage
      dockerfile: Dockerfile
    networks:
      - app-network
    depends_on:
      - mongo-storage
    env_file:
      - .env
    environment:
      - MONGO_HOST=mongo-storage
      - MONGO_PORT=27017
      - MONGO_DATABASE=${STORAGE_DB}

  mongo-storage:
    image: mongo:latest
    networks:
      - app-network
    volumes:
      - mongo-storage-data:/data/db

  nginx:
    image: nginx:latest
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    ports:
      - "80:80"
    depends_on:
      - auth
      - models
      - storage
    networks:
      - app-network
    restart: unless-stopped

volumes:
  mongo-auth-data:
  mongo-models-data:
  mongo-storage-data:

networks:
  app-network:
    driver: bridge
